# شرح نظام معرف الجهاز (Hardware ID - HWID)

## نظرة عامة

معرف الجهاز هو الرقم الفريد الذي يربط الترخيص بجهاز معين. يعتمد النظام على:
- `node-machine-id` للحصول على معرف الجهاز من النظام
- `SHA-256` لإنشاء hash فريد ومشفر
- `Salt` مخصص لكل تطبيق لمنع التصادم

---

## 1. كيف يعمل معرف الجهاز

### 1.1 الحصول على معرف الجهاز

```javascript
// في licenseManager.ts
import { machineIdSync } from 'node-machine-id'

function generateHWID() {
  try {
    // الخطوة 1: الحصول على machine ID من النظام
    const machineId = machineIdSync()
    
    // الخطوة 2: إنشاء hash مشفر
    const hash = createHash('sha256')
    hash.update(machineId + APP_SALT) // إضافة salt فريد
    
    // الخطوة 3: إرجاع أول 32 حرف
    return hash.digest('hex').substring(0, 32)
  } catch (error) {
    console.error('Error generating HWID:', error)
    
    // Fallback: استخدام معلومات النظام
    const fallbackData = `${process.platform}-${process.arch}-${Date.now()}`
    const hash = createHash('sha256')
    hash.update(fallbackData + APP_SALT)
    return hash.digest('hex').substring(0, 32)
  }
}
```

### 1.2 مسار البيانات

```
┌─────────────────────────────────────────────────────────────────┐
│                     machineIdSync()                              │
│         (يحصل على ID فريد من نظام التشغيل)                       │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                      machineId                                   │
│         "a1b2c3d4e5f6..." (معرف الجهاز الخام)                  │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│              APP_SALT = 'dental-clinic-license...'              │
│                    (salt فريد للتطبيق)                          │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│              machineId + APP_SALT                                │
│         "a1b2c3d4... + dental-clinic-license..."               │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                   SHA-256 Hash                                   │
│    "3a7b8c9d0e1f..." (64 حرف hexadecimal)                      │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│              HWID = first 32 chars                              │
│              "3a7b8c9d0e1f..." (32 حرف)                        │
│                    معرف الجهاز النهائي                          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. إظهار معرف الجهاز للمستخدم

### 2.1 في LicenseEntryScreen.tsx

```typescript
interface MachineInfo {
  hwid: string      // معرف الجهاز
  platform: string  // نظام التشغيل (win32, darwin, linux)
  arch: string      // المعمارية (x64, arm64)
}

export default function LicenseEntryScreen({
  machineInfo  // يُمرر من useLicense hook
}: LicenseEntryScreenProps) {
  return (
    <div>
      {/* عرض معرف الجهاز */}
      <div className="device-info">
        <label>معرف الجهاز:</label>
        <code>{machineInfo.hwid}</code>
        
        {/* زر النسخ */}
        <button
          onClick={() => navigator.clipboard.writeText(machineInfo.hwid)}
        >
          📋 نسخ
        </button>
      </div>
      
      {/* معلومات إضافية */}
      <div className="system-info">
        <p>النظام: {machineInfo.platform}</p>
        <p>المعمارية: {machineInfo.arch}</p>
      </div>
      
      <p className="hint">
        💡 أرسل هذا المعرف للمطور للحصول على مفتاح ترخيص
      </p>
    </div>
  )
}
```

### 2.2 في واجهة منفصلة لعرض HWID

```typescript
// ShowHWIDDialog.tsx
import { useLicense } from '../hooks/useLicense'

export default function ShowHWIDDialog({ onClose }) {
  const { machineInfo, getMachineInfo } = useLicense()
  
  // تحديث معلومات الجهاز
  useEffect(() => {
    if (!machineInfo) {
      getMachineInfo()
    }
  }, [])
  
  return (
    <div className="dialog">
      <h2>معرف جهازك</h2>
      
      <div className="hwid-box">
        <div className="hwid-label">معرف الجهاز الفريد:</div>
        <code className="hwid-value">
          {machineInfo?.hwid || 'جاري التحميل...'}
        </code>
        
        <button
          onClick={() => {
            if (machineInfo?.hwid) {
              navigator.clipboard.writeText(machineInfo.hwid)
              alert('تم نسخ معرف الجهاز!')
            }
          }}
          disabled={!machineInfo?.hwid}
        >
          📋 نسخ معرف الجهاز
        </button>
      </div>
      
      <div className="system-info">
        <p><strong>معلومات النظام:</strong></p>
        <ul>
          <li>نظام التشغيل: {machineInfo?.platform}</li>
          <li>المعمارية: {machineInfo?.arch}</li>
        </ul>
      </div>
      
      <div className="instructions">
        <h3>كيفية الحصول على ترخيص:</h3>
        <ol>
          <li>انسخ معرف الجهاز أعلاه</li>
          <li>تواصل مع المطور عبر الواتساب</li>
          <li>أرسل له معرف الجهاز</li>
          <li>سيعطيك مفتاح ترخيص خاص بجهازك</li>
          <li>أدخل المفتاح في الحقل التالي</li>
        </ol>
      </div>
      
      <div className="contact-info">
        <p>📞 المطور: 00963940046997</p>
      </div>
      
      <button onClick={onClose}>إغلاق</button>
    </div>
  )
}
```

---

## 3. سكريبت لعرض معرف الجهاز (للاختبار)

### 3.1 سكريبت Node.js

```javascript
// scripts/showDeviceId.js
/**
 * سكريبت لعرض معرف الجهاز
 * التشغيل: node scripts/showDeviceId.js
 */

const crypto = require('crypto')

// نفس APP_SALT المستخدم في licenseManager
const APP_SALT = 'dental-clinic-license-salt-2025'

function getMachineId() {
  try {
    const { machineIdSync } = require('node-machine-id')
    return machineIdSync()
  } catch (error) {
    console.error('node-machine-id not available:', error.message)
    return null
  }
}

function generateHWID(machineId) {
  if (!machineId) {
    // Fallback
    const fallbackData = `${process.platform}-${process.arch}-${Date.now()}`
    const hash = crypto.createHash('sha256')
    hash.update(fallbackData + APP_SALT)
    return {
      hwid: hash.digest('hex').substring(0, 32),
      source: 'fallback',
      data: fallbackData
    }
  }
  
  const hash = crypto.createHash('sha256')
  hash.update(machineId + APP_SALT)
  
  return {
    hwid: hash.digest('hex').substring(0, 32),
    source: 'machine-id',
    machineId: machineId
  }
}

// تشغيل السكريبت
console.log('='.repeat(60))
console.log('🔍 البحث عن معرف الجهاز...')
console.log('='.repeat(60))

const machineId = getMachineId()
const result = generateHWID(machineId)

console.log('\n📋 نتائج معرف الجهاز:')
console.log('-'.repeat(40))
console.log(`معرف الجهاز (HWID): ${result.hwid}`)
console.log(`مصدر البيانات: ${result.source}`)
console.log(`نظام التشغيل: ${process.platform}`)
console.log(`المعمارية: ${process.arch}`)
console.log(`إصدار Node: ${process.version}`)
console.log('-'.repeat(40))

if (result.machineId) {
  console.log('\n🔐 Machine ID الأصلي:')
  console.log(result.machineId)
}

console.log('\n💡 انسخ هذا المعرف وأرسله للمطور للحصول على ترخيص')
console.log('='.repeat(60))
```

### 3.2 تشغيل السكريبت

```bash
# تشغيل من مجلد المشروع
cd c:/Users/OMAR/Desktop/Dental-Clinic-new

# تشغيل سكريبت عرض HWID
node scripts/showDeviceId.js

# أو باستخدام سكريبت مخصص
node scripts/getDeviceId.js
```

---

## 4. توليد مفتاح ترخيص لجهاز معين

### 4.1 سكريبت التوليد

```javascript
// scripts/generateKeyForDevice.js
/**
 * توليد مفتاح ترخيص لجهاز معين
 * التشغيل: node scripts/generateKeyForDevice.js <device-id> [type] [region]
 */

const crypto = require('crypto')
const { deviceBoundGenerator } = require('../electron/deviceBoundLicenseGenerator.js')

// استلام المعاملات
const deviceId = process.argv[2]
const licenseType = process.argv[3] || 'PROFESSIONAL'
const region = process.argv[4] || 'GLOBAL'

if (!deviceId) {
  console.log('❌ يجب تحديد معرف الجهاز')
  console.log('💡 الاستخدام: node scripts/generateKeyForDevice.js <device-id> [type] [region]')
  console.log('💡 مثال: node scripts/generateKeyForDevice.js 3a7b8c9d0e1f PROFESSIONAL GLOBAL')
  process.exit(1)
}

console.log('='.repeat(60))
console.log('🔑 توليد مفتاح ترخيص لجهاز معين')
console.log('='.repeat(60))
console.log(`معرف الجهاز: ${deviceId}`)
console.log(`نوع الترخيص: ${licenseType}`)
console.log(`المنطقة: ${region}`)
console.log('-'.repeat(60))

// توليد المفتاح
const license = deviceBoundGenerator.generateForDevice(deviceId, {
  licenseType: licenseType,
  region: region,
  isLifetime: true,
  maxDevices: 1,
  generatedAt: new Date().toISOString()
})

console.log('\n✅ تم توليد المفتاح بنجاح!')
console.log(`\n🔑 مفتاح الترخيص:\n`)
console.log(`   ${license.licenseKey}`)
console.log(`\n📋 معلومات الترخيص:`)
console.log(`   النوع: ${license.metadata.licenseType}`)
console.log(`   المنطقة: ${license.metadata.region}`)
console.log(`   مدى الحياة: ${license.metadata.isLifetime}`)
console.log(`   أجهزة كحد أقصى: ${license.metadata.maxDevices}`)
console.log('-'.repeat(60))

// حفظ في ملف
const fs = require('fs')
const outputFile = `license-${deviceId.substring(0, 8)}.json`

const licenseData = {
  licenseKey: license.licenseKey,
  deviceId: deviceId,
  metadata: license.metadata,
  generatedAt: license.generatedAt
}

fs.writeFileSync(outputFile, JSON.stringify(licenseData, null, 2))
console.log(`\n💾 تم حفظ المفتاح في: ${outputFile}`)
console.log('='.repeat(60))
```

### 4.2 تشغيل السكريبت

```bash
# الطريقة 1: استخدام معرف جهاز موجود
node scripts/generateKeyForDevice.js 3a7b8c9d0e1f23456789abcdef123456 PROFESSIONAL GLOBAL

# الطريقة 2: توليد مفتاح للجهاز الحالي
node scripts/generateDeviceBoundLicenses.js test

# الطريقة 3: استخدام سكريبت مخصص
node scripts/generateCurrentDeviceKey.js
```

---

## 5. ملف showCurrentDeviceKey.js

```javascript
// scripts/showCurrentDeviceKey.js
/**
 * إظهار مفتاح الجهاز الحالي
 * هذا المفتاح يعمل فقط على هذا الجهاز
 */

const { deviceBoundGenerator } = require('../electron/deviceBoundLicenseGenerator.js')

console.log('='.repeat(60))
console.log('🔑 مفتاح الترخيص للجهاز الحالي')
console.log('='.repeat(60))

// الحصول على معرف الجهاز الحالي
const currentDeviceId = deviceBoundGenerator.getCurrentDeviceId()
console.log(`\n📱 معرف الجهاز الحالي:`)
console.log(`   ${currentDeviceId}`)
console.log('')

// توليد مفتاح للجهاز الحالي
const license = deviceBoundGenerator.generateForCurrentDevice({
  licenseType: 'PROFESSIONAL',
  region: 'GLOBAL',
  purpose: 'self-activation'
})

console.log('🔐 مفتاح الترخيص الخاص بجهازك:')
console.log('')
console.log('   ╔═══════════════════════════════════════════════════╗')
console.log('   ║                                                   ║')
console.log(`   ║   ${license.licenseKey}              ║`)
console.log('   ║                                                   ║')
console.log('   ╚═══════════════════════════════════════════════════╝')
console.log('')

console.log('📋 معلومات الترخيص:')
console.log(`   • نوع الترخيص: ${license.metadata.licenseType}`)
console.log(`   • المنطقة: ${license.metadata.region}`)
console.log(`   • مدة الصلاحية: مدى الحياة`)
console.log(`   • عدد الأجهزة: ${license.metadata.maxDevices}`)
console.log('')

console.log('⚠️ ملاحظات مهمة:')
console.log('   • هذا المفتاح يعمل فقط على هذا الجهاز')
console.log('   • لا يمكن استخدامه على أجهزة أخرى')
console.log('   • احتفظ بهذا المفتاح في مكان آمن')
console.log('')

console.log('💡 انسخ المفتاح أعلاه وأدخله في شاشة تفعيل الترخيص')
console.log('='.repeat(60))
```

---

## 6. التحقق من HWID في التحقق من الترخيص

### 6.1 التحقق عند التفعيل

```javascript
// في licenseManager.js
async function activateLicense(licenseKey) {
  // 1. التحقق من تنسيق المفتاح
  if (!validateLicenseFormat(licenseKey)) {
    return { isValid: false, error: 'Invalid license format' }
  }

  // 2. الحصول على HWID الحالي
  const currentHWID = generateHWID()

  // 3. التحقق من المفتاح
  const isValid = validateKey(licenseKey, currentHWID)

  if (isValid) {
    // 4. حفظ الترخيص
    const licenseData = {
      license: licenseKey,
      hwid: currentHWID,  // حفظ HWID مع الترخيص
      timestamp: Date.now(),
      activated: true
    }

    await storeLicense(licenseData)

    return {
      isValid: true,
      licenseData: licenseData
    }
  } else {
    return {
      isValid: false,
      error: 'License is not valid for this device'
    }
  }
}
```

### 6.2 التحقق عند التشغيل

```javascript
// في licenseManager.js
async function validateStoredLicense() {
  try {
    // 1. قراءة البيانات المشفرة
    const encryptedData = licenseStore.get('licenseData')
    if (!encryptedData) {
      return { isValid: false, error: 'No license found' }
    }

    // 2. فك التشفير
    const licenseData = decryptLicenseData(encryptedData)

    // 3. الحصول على HWID الحالي
    const currentHWID = generateHWID()

    // 4. التحقق من التطابق
    if (licenseData.hwid !== currentHWID) {
      return {
        isValid: false,
        error: 'License is bound to a different device'
      }
    }

    // 5. التحقق من التفعيل
    if (!licenseData.activated) {
      return {
        isValid: false,
        error: 'License is not activated'
      }
    }

    return {
      isValid: true,
      licenseData: licenseData
    }

  } catch (error) {
    return {
      isValid: false,
      error: 'License validation failed'
    }
  }
}
```

---

## 7. مخطط تدفق HWID

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          تشغيل التطبيق                                    │
└─────────────────────────────────┬───────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                   generateHWID()                                         │
│              (إنشاء معرف الجهاز الحالي)                                   │
└─────────────────────────────────┬───────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                   licenseStore.get('licenseData')                        │
│                   (قراءة بيانات الترخيص المحفوظة)                         │
└─────────────────────────────────┬───────────────────────────────────────┘
                                  │
                    ┌─────────────┴─────────────┐
                    │                           │
                    ▼                           ▼
            ┌───────────────┐           ┌───────────────────┐
            │ لا توجد بيانات │           │ توجد بيانات       │
            │ (التشغيل الأول)│           │ (ترخيص موجود)     │
            └───────┬───────┘           └─────────┬─────────┘
                    │                             │
                    ▼                             ▼
            ┌───────────────┐           ┌───────────────────┐
            │ إظهار شاشة   │           │ decryptLicense()  │
            │ إدخال الترخيص │           │ (فك تشفير البيانات)│
            └───────┬───────┘           └─────────┬─────────┘
                    │                             │
                    │                             ▼
                    │                   ┌───────────────────┐
                    │                   │ التحقق من HWID   │
                    │                   │ (المقارنة)        │
                    │                   └─────────┬─────────┘
                    │                             │
                    │             ┌─────────────┴─────────────┐
                    │             │                           │
                    │             ▼                           ▼
                    │     ┌───────────────┐           ┌───────────────┐
                    │     │ HWID متطابق │           │ HWID مختلف    │
                    │     │ ✅_pass     │           │ ❌_fail      │
                    │     └───────┬───────┘           └───────┬───────┘
                    │             │                           │
                    │             ▼                           ▼
                    │     ┌───────────────┐           ┌───────────────┐
                    │     │ تحميل التطبيق │           │ إظهار رسالة  │
                    │     │               │           │ خطأ          │
                    │     └───────────────┘           └───────────────┘
                    │
                    ▼
            ┌───────────────────────────────────────────────────────────┐
            │                   إدخال مفتاح الترخيص                       │
            │                   activateLicense(key)                      │
            └─────────────────────────────────┬─────────────────────────┘
                                              │
                                              ▼
                                    ┌───────────────────┐
                                    │ validateKey(key,  │
                                    │   currentHWID)    │
                                    └─────────┬─────────┘
                                              │
                                              ▼
                                    ┌───────────────────┐
                                    │ ✅ مفتاح صحيح    │
                                    │ ❌ مفتاح خاطئ    │
                                    └─────────┬─────────┘
                                              │
                                              ▼
                                    ┌───────────────────┐
                                    │ storeLicense(     │
                                    │   key, HWID)      │
                                    │ (حفظ الترخيص)     │
                                    └───────────────────┘
```

---

## 8. أمثلة على HWID

### 8.1 أمثلة HWID حقيقية

```
Device 1: 3a7b8c9d0e1f23456789abcdef123456
Device 2: a1b2c3d4e5f67890123456789abcdef1
Device 3: 9f8e7d6c5b4a3210987654321fedcba9
```

### 8.2 مثال على مفتاح ترخيص

```
معرف الجهاز: 3a7b8c9d0e1f23456789abcdef123456
مفتاح الترخيص: ABC12-DEF34-GHI56-JKL78

⚠️ هذا المفتاح يعمل فقط على الجهاز أعلاه
```

---

## 9. الأخطاء المتعلقة بـ HWID

| الخطأ | السبب | الحل |
|-------|-------|------|
| "License is bound to a different device" | تم تفعيل المفتاح على جهاز آخر | تحرير المفتاح من الجهاز القديم أو توليد مفتاح جديد |
| "Invalid HWID" | مشكلة في قراءة machine-id | استخدام fallback |
| "HWID mismatch" | تغيير في مكونات الجهاز | التواصل مع المطور |

---

## 10. ملف getDeviceId.js الكامل

```javascript
/**
 * getDeviceId.js - سكريبت الحصول على معرف الجهاز
 * 
 * الوظائف:
 * 1. الحصول على معرف الجهاز الفريد
 * 2. إنشاء مفتاح ترخيص للجهاز
 * 3. حفظ المفتاح في ملف
 * 
 * التشغيل:
 *   node scripts/getDeviceId.js
 *   node scripts/getDeviceId.js generate
 *   node scripts/getDeviceId.js generate PROFESSIONAL GLOBAL
 */

const crypto = require('crypto')
const fs = require('fs')
const path = require('path')

// إعدادات النظام
const APP_NAME = 'Dental Clinic'
const APP_SALT = 'dental-clinic-license-salt-2025'
const MASTER_KEY = 'DENTAL_CLINIC_MASTER_KEY_2025_SECURE_ENCRYPTION'

/**
 * الحصول على معرف الجهاز من النظام
 */
function getMachineId() {
  try {
    const { machineIdSync } = require('node-machine-id')
    return machineIdSync()
  } catch (error) {
    console.warn('⚠️ node-machine-id not available, using fallback')
    return null
  }
}

/**
 * إنشاء HWID من معرف الجهاز
 */
function generateHWID(machineId) {
  if (!machineId) {
    // Fallback: استخدام معلومات النظام
    const fallbackData = `${process.platform}-${process.arch}-${Date.now()}`
    const hash = crypto.createHash('sha256')
    hash.update(fallbackData + APP_SALT)
    return {
      hwid: hash.digest('hex').substring(0, 32),
      source: 'fallback',
      data: fallbackData
    }
  }

  const hash = crypto.createHash('sha256')
  hash.update(machineId + APP_SALT)

  return {
    hwid: hash.digest('hex').substring(0, 32),
    source: 'machine-id',
    machineId: machineId
  }
}

/**
 * إنشاء مفتاح ترخيص خوارزمي
 */
function generateLicenseKey(deviceId, licenseType = 'PROFESSIONAL') {
  const seed = deviceId + MASTER_KEY + licenseType
  const baseHash = crypto.createHash('sha256').update(seed).digest('hex')

  const part1 = baseHash.substring(0, 5).toUpperCase()
  const part2 = baseHash.substring(8, 13).toUpperCase()
  const part3 = baseHash.substring(16, 21).toUpperCase()
  const part4 = baseHash.substring(24, 29).toUpperCase()

  return `${part1}-${part2}-${part3}-${part4}`
}

/**
 * التحقق من مفتاح ترخيص
 */
function validateLicenseKey(licenseKey, deviceId) {
  const licenseTypes = ['STANDARD', 'PROFESSIONAL', 'ENTERPRISE', 'PREMIUM', 'ULTIMATE']

  for (const type of licenseTypes) {
    const expectedKey = generateLicenseKey(deviceId, type)
    if (expectedKey === licenseKey) {
      return { valid: true, type: type }
    }
  }

  return { valid: false }
}

/**
 * حفظ بيانات الترخيص
 */
function saveLicenseData(deviceId, licenseKey, metadata = {}) {
  const data = {
    deviceId: deviceId,
    licenseKey: licenseKey,
    metadata: {
      licenseType: metadata.type || 'PROFESSIONAL',
      region: metadata.region || 'GLOBAL',
      isLifetime: true,
      maxDevices: 1,
      createdAt: new Date().toISOString(),
      ...metadata
    }
  }

  const filename = `device-license-${deviceId.substring(0, 8)}.json`
  const filepath = path.join(__dirname, '..', filename)

  fs.writeFileSync(filepath, JSON.stringify(data, null, 2), 'utf8')

  return { filepath, data }
}

/**
 * الدالة الرئيسية
 */
function main() {
  const args = process.argv.slice(2)
  const command = args[0] || 'show'

  console.log('='.repeat(70))
  console.log(`🔐 ${APP_NAME} - نظام إدارة التراخيص`)
  console.log('='.repeat(70))
  console.log('')

  // الحصول على معرف الجهاز
  const machineId = getMachineId()
  const hwidResult = generateHWID(machineId)
  const deviceId = hwidResult.hwid

  console.log('📱 معلومات الجهاز:')
  console.log('-'.repeat(50))
  console.log(`معرف الجهاز (HWID): ${deviceId}`)
  console.log(`مصدر المعرف: ${hwidResult.source}`)
  console.log(`نظام التشغيل: ${process.platform}`)
  console.log(`المعمارية: ${process.arch}`)
  console.log(`إصدار Node: ${process.version}`)
  console.log('-'.repeat(50))
  console.log('')

  if (command === 'generate') {
    // توليد مفتاح ترخيص
    const licenseType = args[1] || 'PROFESSIONAL'
    const region = args[2] || 'GLOBAL'

    console.log('🔑 جاري توليد مفتاح ترخيص...')
    console.log(`نوع الترخيص: ${licenseType}`)
    console.log(`المنطقة: ${region}`)
    console.log('')

    const licenseKey = generateLicenseKey(deviceId, licenseType)

    console.log('✅ تم توليد المفتاح بنجاح!')
    console.log('')
    console.log('   ╔══════════════════════════════════════════════════════════╗')
    console.log('   ║                                                          ║')
    console.log(`   ║   ${licenseKey}                                ║`)
    console.log('   ║                                                          ║')
    console.log('   ╚══════════════════════════════════════════════════════════╝')
    console.log('')

    // حفظ في ملف
    const result = saveLicenseData(deviceId, licenseKey, {
      type: licenseType,
      region: region
    })

    console.log(`💾 تم حفظ بيانات الترخيص في:`)
    console.log(`   ${result.filepath}`)
    console.log('')

    // التحقق
    const validation = validateLicenseKey(licenseKey, deviceId)
    console.log(`✅ التحقق: ${validation.valid ? 'ناجح' : 'فاش'}`)
    if (validation.valid) {
      console.log(`   نوع الترخيص: ${validation.type}`)
    }

  } else if (command === 'validate') {
    // التحقق من مفتاح
    const licenseKey = args[1]

    if (!licenseKey) {
      console.log('❌ يرجى إدخال مفتاح للتحقق')
      console.log('💡 الاستخدام: node scripts/getDeviceId.js validate <license-key>')
      return
    }

    console.log('🔍 جاري التحقق من المفتاح...')
    console.log(`المفتاح: ${licenseKey}`)
    console.log(`الجهاز: ${deviceId}`)
    console.log('')

    const validation = validateLicenseKey(licenseKey, deviceId)

    if (validation.valid) {
      console.log('✅ المفتاح صالح للجهاز الحالي!')
      console.log(`   نوع الترخيص: ${validation.type}`)
    } else {
      console.log('❌ المفتاح غير صالح للجهاز الحالي')
      console.log('   قد يكون المفتاح لجهاز آخر أو غير صحيح')
    }

  } else {
    // عرض معلومات الجهاز فقط
    console.log('📋 خيارات التشغيل:')
    console.log('')
    console.log('   node scripts/getDeviceId.js')
    console.log('      → إظهار معرف الجهاز فقط')
    console.log('')
    console.log('   node scripts/getDeviceId.js generate')
    console.log('      → توليد مفتاح ترخيص للجهاز الحالي')
    console.log('')
    console.log('   node scripts/getDeviceId.js generate PROFESSIONAL GLOBAL')
    console.log('      → توليد مفتاح بنوع ومنطقة محددين')
    console.log('')
    console.log('   node scripts/getDeviceId.js validate KEY-HERE')
    console.log('      → التحقق من مفتاح ترخيص')
    console.log('')

    console.log('💡 نسخ معرف الجهاز:')
    console.log('   انقر نقرتين على HWID أعلاه للنسخ')
    console.log('   أو استخدم: echo %HWID% | clip')
    console.log('')

    console.log('📞 للحصول على مفتاح ترخيص:')
    console.log('   1. انسخ معرف الجهاز أعلاه')
    console.log('   2. تواصل مع المطور')
    console.log('   3. أرسل له معرف الجهاز')
    console.log('')
  }

  console.log('='.repeat(70))
}

// تشغيل
main()
```

---

## 11. تشغيل السكريبتات

```bash
# 1. تشغيل سكريبت عرض HWID
cd c:/Users/OMAR/Desktop/Dental-Clinic-new
node scripts/getDeviceId.js

# 2. توليد مفتاح للجهاز الحالي
node scripts/getDeviceId.js generate PROFESSIONAL GLOBAL

# 3. التحقق من مفتاح
node scripts/getDeviceId.js validate ABCDE-FGH12-IJK34-LMN56

# 4. تشغيل سكريبت showDeviceId.js
node scripts/showDeviceId.js

# 5. تشغيل showCurrentDeviceKey.js
node scripts/showCurrentDeviceKey.js
```

هذا الملف يحتوي على كل ما تحتاجه لفهم وعرض واستخدام نظام معرف الجهاز في نظام التراخيص.
